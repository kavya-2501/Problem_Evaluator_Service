export const PYTHON_IMAGE="python";

// docker stream header will contain type of stream i.e stdout or stderr
export const DOCKER_STREAM_HEADER_SIZE=8; //the header size of docker size, it will contain data about stream